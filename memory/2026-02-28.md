# 2026-02-28

## Signal Engine Health Check
- Engine running OK: 6 pairs, dryRun: true, ~49MB memory
- Found error spam: `BTC/USDT:SHORT` position ghost causing `bitget does not have market symbol BTC:USDT:USDT` every 30s
- Root cause: `virtual-tracker.js` `reloadPositions()` was constructing bitgetSymbol wrong — `row.pair.replace('/', ':') + ':USDT'` → `BTC:USDT:USDT` instead of `BTC/USDT:USDT`
- Fix: changed to `row.pair + ':USDT'` (commit `044642a`, pushed to `pulsewave-labs/pulsewave-engine`)
- Ghost position was from the deleted BTC SHORT signal — loaded on startup before deletion, stuck in memory
- Supabase confirmed clean (no active signals)
- Redeploy cleared the ghost + fix prevents future recurrence

## Trend Buddy V1 → V2
- Built Trend Buddy V1 (ADX + EMA + structure voting) — Mason didn't like it, called it generic
- Mason referenced InSilico Terminal and their "Crayons" indicator as inspiration
- InSilico site is a closed SPA, couldn't scrape docs for Crayons specifically
- Built V2 with completely different philosophy:
  - Adaptive Supertrend (EMA-smoothed ATR) as primary
  - Bollinger Band width squeeze for compression/ranging detection
  - Volume impulse for confirmation
  - Orange compression zone boxes painted on chart
  - Breakout signals (💥) from compression = highest probability
  - MTF confluence (auto-detects 4× current TF)
- File: `pinescript/TrendBuddy-V2.pine`

## PulseWave Scalper Strategy
- Mason asked for "god tier" BTC scalping strategy focused on risk management
- Built as `strategy()` (not indicator) for instant backtesting
- Risk-first design:
  - Fixed 1% risk per trade, position sized from stop distance
  - Structure-based stops (micro swing + ATR buffer)
  - Partial exits: TP1 at 1.5R closes 50%, rest trails to TP2 at 3R
  - Time stop: 20 bars max if TP1 not hit
  - Daily loss limit: -3% stops trading
  - Max 6 trades/day
- 3 entry types: micro structure break, VWAP bounce, EMA cross
- Filters: session (London/NY), volatility (ATR range), trend (50 EMA)
- Includes 0.04% commission and slippage in backtest
- Dashboard with live stats (WR, PF, DD, daily P&L, status)
- File: `pinescript/PulseScalper.pine`
- Waiting for Mason to backtest and send results for optimization
