# 2026-02-22

## PulseWave Indicator V1.2 — SpacemanBTC Merge Complete

- Mason sent full SpacemanBTC key levels PineScript across multiple messages (v5 indicator)
- Merged into PulseWave Indicator V1.2 at `pinescript/PulseWaveIndicator-V1.2.pine`
- Combined: Part 1 (S/R Engine, 4 TFs) + Part 2 (IDWM Key Levels)
- Key levels include: 4H, Daily, Monday Range, Weekly, Monthly, Quarterly, Yearly opens/H/L/mids + FX Sessions (London, NY, Asia)
- Hit v6 compilation error: `time()` returns `int` not `bool` — fixed with `not na(time(...))` wrapper
- Mason asked to "improve and refine" the IDWM section

### IDWM Refinements Added
1. **Price + Distance % in labels** — e.g. "Weekly Open 97,450 (-1.23%)"
2. **Auto-fade distant levels** — transparency increases beyond configurable % threshold (default 5%)
3. **Midpoints as dashed lines** — visually distinct from solid open/H/L lines
4. **H/L Zone fills** — semi-transparent boxes between Prev High/Low (toggle)
5. **Previous Close levels** — PDC, PWC, PMC added (missing from original SpacemanBTC)
6. **Smart color fading** via `f_KLFade()` function
7. **isMid parameter** in `f_DrawKL()` for cleaner code

### File Location
- `pinescript/PulseWaveIndicator-V1.2.pine` — the merged + refined version
