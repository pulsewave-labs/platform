# 2026-03-04

## PulseWave Edge Strategy V1 Backtest Results (from Mason's CSV)
- 850+ trades (5,034 exits including margin call fragments), Mar 2025 → Mar 2026
- **Net P&L: -$9,876 (-98.8%)** — complete blowup
- Win rate: 18.6%, R:R 1.44:1, profit factor 0.328
- **Root causes identified:**
  1. **Margin call hell** — 41.5% of trades were margin calls. `default_qty_value=100` (100% equity) + tight stops = micro-lot fragments getting instantly margin called. 80% of MCs were tiny positions <$100.
  2. **Overtrading** — 13.7 trades/day avg, max 26/day. Death by a thousand cuts.
  3. **Bad R:R math** — 1.44:1 at 18.6% WR needs 3:1 minimum to break even.
- Longs: 31.8% WR, Shorts: 12.0% WR (shorts much worse)
- Max consecutive losses: 41
- Monthly losses shrink over time (Mar -$3.7K → Dec -$120) suggesting some adaptation

## PulseWave Edge Strategy V2 Built
- File: `pinescript/PulseWaveEdge-Strategy-V2.pine` (commit `46395b7`)
- Key fixes:
  - Risk-based position sizing (1.5% equity per trade via SL distance calc)
  - ADX min filter (15), volume filter (0.8x MA), ATR volatility filter
  - SL distance bounds (0.15% min, 1.5% max) — rejects noise and overly wide stops
  - 5-bar cooldown between trades
  - 3% daily loss limit with force-close
  - Zones must have 1+ prior touch
  - Stronger candle patterns (1.2x engulfing, min body 30% ATR)
  - Kijun bounce SL = kijun ± 0.5×ATR (not full kijun range)
  - R:R 2.5, time stop 30 bars
- Mason needs to backtest this version

## PulseWave Zones Indicator Built
- File: `pinescript/PulseWaveZones.pine`
- Pure S/R zone indicator (no signals/strategy)
- Features: ATR-adaptive zone width, Rule of Fives depletion, S/R flip detection, fresh zone highlighting, strength scoring, auto-merge, MTF zones, proximity alerts, dashboard
- **Pine v6 issues encountered and fixed:**
  - Can't use history operator on UDT fields (`z.top[1]` → use local var instead)
  - Can't assign fields inline from `array.get()` → assign to local var first
  - Storing box/label refs in UDT caused "static background" rendering bug
  - **Final fix:** Rewrote to flat parallel arrays, draw only on `barstate.islast`, delete all boxes/labels each bar then recreate (commit `d60383d`)
- Mason testing — reported zones not sticking to candles on scroll, sent 3 fix iterations
- `zStrength` array out of bounds on bar 0 — fixed with `while` growth loop (commit `6791fba`)
- Mason still testing latest version

## PW Zones Y-Axis Scaling Saga (ongoing, ~6:30 AM UTC)
- **Core issue**: Zones/lines don't move with candles when Mason manually drags Y-axis price scale
- **15+ versions tried** — boxes, lines, linefill, plot+fill, xloc.bar_time, xloc.bar_index, scale=scale.none, persistent boxes, UDT with box refs, batch delete + recreate, var array slots
- Mason sent V1.1 full source code — I rebuilt PW Zones using V1.1's EXACT engine:
  - Same `calcSR()` clustering algorithm
  - Same `request.security()` per-timeframe pattern
  - Same packed float returns (mid/hi/lo/strength × 5 levels)
  - Same `var` line/label arrays, delete old → set new in same slot
  - `max_boxes_count=0` (no boxes)
  - `barstate.islast` drawing only
- **Even the V1.1-clone doesn't scale for Mason** — strongly suggests it's NOT a code issue
- **Current theory**: TradingView is caching a broken scale assignment from the 15+ code swaps. Mason needs to hard refresh browser (Ctrl+Shift+R), remove indicator, re-add fresh
- Mason is extremely frustrated (understandably — 15 tries)
- If hard refresh doesn't fix it, V1.1 itself might also not scale (Mason may not have noticed before since he doesn't routinely drag Y-axis)
- **Next step**: Mason to try hard refresh + fresh add. If V1.1 also doesn't scale fresh, this is a TradingView behavior, not a code bug

## BTC Market Read (5:25 AM UTC)
- Price: $67,829, down ~1% 24h
- 7-day range: $65,713 – $68,864
- Structure: lower highs within range ($68.8K → $68.3K → $67.8K), double bottom at $65.7K
- Called lower in next 12h — $65.7K support depleting (Rule of Fives), sellers above $68K
